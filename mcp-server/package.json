{
  "name": "pdf-inspector-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP and HTTP service that routes PDFs through Firecrawl pdf-inspector with optional OCR fallback.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/http.mjs",
    "start:http": "node src/http.mjs",
    "start:stdio": "node src/stdio.mjs",
    "check": "node --check src/security.mjs && node --check src/pdf.mjs && node --check src/server.mjs && node --check src/http.mjs && node --check src/stdio.mjs",
    "test": "npm run check && node --test"
  },
  "dependencies": {
    "@firecrawl/pdf-inspector": "1.11.2",
    "@modelcontextprotocol/sdk": "1.30.0",
    "express": "5.1.0",
    "zod": "4.4.3"
  }
}
